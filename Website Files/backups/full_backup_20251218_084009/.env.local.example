# Vortex PCs - Local Development Environment
# Copy this file to .env.local and fill in values as needed.
# For full local API testing, use `vercel dev` after setting these.

# ---------------------------------------------
# Client (Vite) - Firebase (optional)
# ---------------------------------------------
VITE_FIREBASE_API_KEY=
VITE_FIREBASE_AUTH_DOMAIN=
VITE_FIREBASE_PROJECT_ID=

# ---------------------------------------------
# Server - Firebase Admin (choose one approach)
# ---------------------------------------------
# 1) Explicit credentials
FIREBASE_PROJECT_ID=
FIREBASE_CLIENT_EMAIL=
# Note: preserve newlines as \n in quotes
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"

# 2) Base64-encoded service account JSON (alternative)
FIREBASE_SERVICE_ACCOUNT_BASE64=

# ---------------------------------------------
# SMTP for scheduled emails (optional)
# ---------------------------------------------
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=

# ---------------------------------------------
# Cron security for scheduled tasks
# ---------------------------------------------
CRON_SECRET=

# ---------------------------------------------
# Public base URLs (used by some APIs)
# ---------------------------------------------
PUBLIC_BASE_URL=http://localhost:4173
NEXT_PUBLIC_BASE_URL=http://localhost:4173

# ---------------------------------------------
# Stripe (optional)
# ---------------------------------------------
STRIPE_SECRET_KEY=
VITE_STRIPE_PUBLISHABLE_KEY=
VITE_STRIPE_BACKEND_URL=

# Quick start for local API testing:
# 1) cp .env.local.example .env.local
# 2) Fill the values above (at least PUBLIC_BASE_URL and CRON_SECRET)
# 3) Install Vercel CLI: npm i -g vercel
# 4) Run: vercel dev
#    - This serves your /api/* endpoints locally
#    - Browse the provided localhost URL to exercise API routes
